import 'package:flutter/material.dart';

class RotateScreen extends StatelessWidget {
  const RotateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('rotate screen');
  }
}
